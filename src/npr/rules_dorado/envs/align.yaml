name: ont-align
channels:
  - conda-forge
  - bioconda
dependencies:
  - python >= 3.10
  - samtools == 1.19.2
  - pycoqc == 2.5.0.3
  - pysam == 0.22.0
  - jupyter_highlight_selected_word # dep for pandas
